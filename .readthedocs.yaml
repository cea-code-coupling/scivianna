# .readthedocs.yaml
version: 2

build:
  os: ubuntu-24.04
  jobs:
    pre_create_environment:
      - cp tutorials/*.ipynb docs/source/tutorials/
      
  tools:
    python: "3.12"

python:
  install:
    - method: pip
      path: .

sphinx:
  configuration: docs/source/conf.py